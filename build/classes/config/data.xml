<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"mybatis-3-mapper.dtd">

<!-- namespace 를 mabatis 에선 필수로 해야 함!!! -->
<mapper namespace="data">
	<resultMap type="datavo" id="dataRM">
		<result property="sensorDataSerialNum" column="sensor_data_serial_num" />
		<result property="sensorDataTimestamp" column="sensor_data_timestamp" />
		<result property="sensorDataValue" column="sensor_data_value" />
		<result property="sensorValue.sensorTypeNum" column="sensor_type_num" />
		<result property="sensorValue.sensorType" column="sensor_type" />
		<result property="sensorValue.unit" column="unit" />
		<result property="deviceId" column="device_id" />
	</resultMap>
	
	<select id="getLastDataByDeviceIdSortedByTimestamp" parameterType="string" resultMap="dataRM">
	    select sensor_data_serial_num, sensor_data_timestamp, sensorDataTimestampStr, 
				sensor_data_value, sensor_type_num, device_id, sensor_type, unit
		from(
				select data.sensor_data_serial_num, data.sensor_data_timestamp, 
						to_char(data.sensor_data_timestamp, 'YYYY/MM/DD HH24:MI:SS') as sensorDataTimestampStr, 
						data.sensor_data_value, data.sensor_type_num, data.device_id, 
						sensor.sensor_type, sensor.unit
				from IOESENSORDATA data, IOESENSOR sensor
				where data.sensor_type_num=sensor.sensor_type_num
				order by data.sensor_data_timestamp desc
		)
		where device_id=#{deviceId} and ROWNUM=1
	</select>
</mapper>
 
























